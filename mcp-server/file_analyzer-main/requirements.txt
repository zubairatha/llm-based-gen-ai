# Core dependencies for MCP File Analyzer
mcp>=1.0.0
pandas>=2.0.0
pyarrow>=10.0.0

# HTTP client dependencies (for http_client.py)
httpx>=0.27.0

# Optional: Alternative parquet engine (if pyarrow has issues)
# fastparquet>=0.8.0

# Development dependencies (uncomment if needed)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0